name: Build Flutter APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GRADLE_OPTS: -Dorg.gradle.jvmargs="-Xmx4096m -XX:MaxMetaspaceSize=512m" -Dorg.gradle.daemon=false

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Recreate Android project folder
        run: |
          cd Mobile
          flutter create --org com.nexcall --platforms=android .

      - name: Patch compileSdkVersion and minSdkVersion
        run: |
          cd Mobile/android/app
          if [ -f "build.gradle" ]; then
            sed -i 's/compileSdk = flutter.compileSdkVersion/compileSdk = 36/' build.gradle
            sed -i 's/compileSdkVersion flutter.compileSdkVersion/compileSdkVersion 36/' build.gradle
            sed -i 's/minSdk = flutter.minSdkVersion/minSdk = 24/' build.gradle
            sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 24/' build.gradle
            echo "Patched build.gradle"
          elif [ -f "build.gradle.kts" ]; then
            sed -i 's/compileSdk = flutter.compileSdkVersion/compileSdk = 36/' build.gradle.kts
            sed -i 's/minSdk = flutter.minSdkVersion/minSdk = 24/' build.gradle.kts
            echo "Patched build.gradle.kts"
          fi

      - name: Accept Android licenses
        run: yes | flutter doctor --android-licenses || true

      - name: Get packages
        run: |
          cd Mobile
          flutter pub get

      - name: Build Release APK
        run: |
          cd Mobile
          flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: nexcall-app-release
          path: Mobile/build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
